<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required for Open-Meteo weather, Supabase sync (M5/M6) and Sentry.
         The app is offline-first, but network access must be declared so the
         release build (debug/ adds it only for hot reload) can reach them. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- connectivity_plus: read the network interface state (online/offline). -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- geolocator (M7): one-shot location at onboarding to derive the H3 cell
         on-device. Coarse only — we just need the ~km-wide neighbourhood (r7
         cell, ~1.2 km edge); fine precision is unnecessary (FR-8). Raw
         coordinates are never stored or synced — only the H3 cells sync, and
         weather uses the cell centroid. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- Local task reminders (M8). POST_NOTIFICATIONS is a runtime prompt on
         Android 13+, requested from the priming screen (21). RECEIVE_BOOT_COMPLETED
         lets the plugin reschedule pending reminders after a reboot.
         SCHEDULE_EXACT_ALARM: reminders must fire at the set time even in Doze
         (inexact alarms were deferred indefinitely on Samsung) — exact timing is
         the core function of a reminder app (Play-policy compliant). On Android
         13+ the user grants it via the system "Alarms & reminders" page. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <!-- Privacy: the local drift DB may hold not-yet-synced data; disable
         Android auto-backup so it isn't copied to the user's Drive. Guest data
         is already device-local by design (lost on app removal / device change). -->
    <application
        android:label="tendask"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- flutter_local_notifications receivers (the plugin's own manifest
             declares none). ScheduledNotificationReceiver is the one AlarmManager
             triggers at the scheduled time to actually post the notification —
             without it scheduled reminders are queued but never fire. The boot
             receiver reschedules pending reminders after a reboot. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false"/>
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver"
            android:exported="false"/>
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
        <!-- FCM (M11.5): background notification messages are rendered by the OS
             itself — route them to the `suggestions` channel (created by
             NotificationService at init) and reuse the M8 status-bar icon. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="suggestions" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_notify" />
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- url_launcher (Android 11+ package visibility): open the privacy
             policy in an external browser. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>
