# M11 — pametni motor (ustavljen)

> **Status: ustavljen 29. 7. 2026 za nedoločen čas** — zavrnjen kot preobsežen, ne opuščen kot ideja.
> Motor se bo začel znova, v majhnih korakih iz `main`. **Po M11 ni M12.**
>
> Ta stran je vse, kar o M11 potrebuješ pri delu na `main`. Specifikacija, koda in delovna zgodovina
> živijo na veji — glej [Kje je vse](#kje-je-vse).

## Kaj je bilo

Plast B obvestil: dnevni strežniški pregled, ki iz agronomskih pravil in vremena sestavi **predloge**
(»čas je za prvo košnjo«) in jih pošlje s pushem — kurirana pravila, brez AI. Zgrajen je bil do faze D
(M11.1–M11.16); odprta je ostala faza E (Okolica: skupnostni percentili + paywall).

**Zgrajen in ugasnjen:** klient prek `kSuggestionsEnabled=false`, strežnik prek
`app_config.engine_enabled=false`. Edge funkcija `smart-engine` **ni deployana**. Nikoli ni bil izdan —
zato je tudi najmočnejši kandidat za Tendask+ (nihče nič ne izgubi, gl. [`narejeno.md`](narejeno.md)).

## Kar M11 pušča na produkciji

Dvoje se dotika **žive baze** in velja ne glede na to, da je motor ustavljen:

1. **Vrzel `0006`–`0010` v ledgerju migracij.** Te datoteke živijo samo na veji, aplicirane pa so bile
   na živo bazo mimo CLI. Nove migracije oštevilči **nad najvišjo** obstoječo; **nikoli ne uporabi
   `0006`–`0010`**. Podrobno: [`deploy-runbook.md`](deploy-runbook.md) §2.
2. **Neodstranljiv pg_cron job `engine-dispatch`** (jobid 1, `*/30 * * * *`). Je v lasti
   `supabase_admin`, zato ga `postgres` ne vidi in ne more odjaviti — ni ga v Dashboard Cron UI.
   Devet dni je metal `function engine_dispatch() does not exist`; **ublažen z no-op stubom**
   `public.engine_dispatch()`, zato zdaj tiho uspe. Ob morebitnem prihodnjem deployu: zamenjaj telo
   stuba, **ne ustvarjaj drugega joba**, za pravi izbris rabiš superuser ali Supabase Support.

## Arhitekturne odločitve, ki jih je vredno obdržati

Veljajo tudi ob ponovnem zagonu — vsaka je bila razrešena z razlogom.

| Odločitev | Zakaj |
|---|---|
| **Motor teče strežniško** (Edge Function + `pg_cron` dispatcher vsakih 30 min za uporabnike, katerih lokalni čas je v oknu 07:00–07:30) | Naprava ne sme računati predlogov — baterija, in pravila se morajo dati popraviti brez nove izdaje aplikacije. Okno je tik po koncu privzetih tihih ur. |
| **Predlogi so navadna user-tabela `suggestion`** — strežnik piše, klient bere prek pulla in piše **samo** `status` + `dismiss_scope` | Pas na Domov tako bere iz drift baze → **offline-first ostane nedotaknjen**. Brez posebne poti za predloge. |
| **`suggestion_log` piše izključno strežnik** | En pisec na tabelo = ni konfliktov, ki bi jih moral reševati LWW. |
| **Vreme za motor se keša per H3-celica per dan** | Uporabniki v isti celici delijo **en** Open-Meteo klic (strošek in usage policy), koordinate so vedno centroid celice — nikoli uporabnikove. |
| **Klimatski profil računa naprava** (Open-Meteo archive na centroid r7) | Rezultat je `climate_profile` (owner-only) + grob javni `climate_bucket`; strežnik ne dobi natančne lokacije. |
| **`task.agg_context` štemplja klient ob `done` od prvega dne** | Kopiči zgodovino za V2 percentile, tudi če pogledi pridejo veliko kasneje. Retroaktivno se je ne da izračunati. |
| **FCM prek HTTP v1** (service account v Supabase secrets) | Legacy server key je ukinjen. Token v `profile.fcm_token`; MVP = zadnja naprava zmaga, multi-device šele kasneje. |
| **V2 agregati z dvema pragoma** (`K_privacy=5`, `K_reliab=30`) | Prvi ščiti zasebnost (premalo ljudi v celici = ni podatka), drugi verodostojnost (številka se pokaže šele, ko kaj pomeni). |

## Kje je vse

Veja **`feat/m11-smart-engine`** je v celoti pushana na origin (`c643320`, 60 commitov) in se hrani
kot arhiv. Na njej je:

- **30 dokumentov v `docs/m11/`** — 12 poglavij specifikacije (agronomska pravila z viri, signalni sloj,
  pravila R1–R7, SQL, drift zrcalo, FCM, klima, Flutter arhitektura, koraki z DoD) + delovna zgodovina.
  **Novejši so od tega, kar je bilo na `main`** — beri tam, ne iz git zgodovine `main`-a.
- **61 agronomskih pravil kot izvršljiv seed** (ne le kot dokument): `tool/seed/plant_task_rules_seed.dart`
  → `supabase/seed/plant_task_rules.sql`, s parity testom.
- Motor (`supabase/functions/smart-engine/`), migracije `0006`–`0022`, drift do `schemaVersion 15`.

```bash
git show origin/feat/m11-smart-engine:docs/m11/01-agronomska-pravila.md   # posamezna datoteka
git switch feat/m11-smart-engine                                          # cela veja
```

> **Ne beri `docs/m11/13-…27-nadaljevanje-prompt.md` na veji.** To so porabljeni delovni nalogi, ki si
> med sabo nasprotujejo (eden se začne z »M11 je DOKONČAN«, kar že takrat ni bilo res). Za stanje
> velja ta stran.
>
> Kar je na `main` živelo do 29. 7. 2026 (zastarel posnetek 13 datotek):
> `git show 3ce81df:docs/m11/<ime>.md`.
