# M11 — najdbe med izvedbo in testiranjem

> **Tekoč dnevnik, ne poročilo.** Vsaka najdba se vpiše **takoj ob odkritju**, ne ob koncu seje —
> sicer preživi samo do naslednjega `/clear`.
> Zapiši: **kaj** (opazljivo dejstvo), **dokaz** (izmerjeno, ne ocenjeno), **kam spada**, **status**.
> Ko je popravljeno, vrstica ostane in dobi hash — dnevnik je zgodovina, ne seznam opravil.
>
> Plan popravkov po pregledu je `17-plan-popravkov.md` (paketi P1–P12). Ta dokument lovi tisto,
> kar se pokaže **med** izvajanjem tega plana in v pregledu ni obstajalo.

---

## Pregled — kaj od tega sploh lahko škodi (2026-07-28)

> Štiriindvajset vrstic je videti kot plaz, dokler jih ne razvrstiš po tem, **česa se
> dotikajo**. Ta tabela je namenjena odločanju; podrobnosti ostajajo spodaj.

| Kaj je prizadeto | Najdbe | Lahko da uporabniku **napačen rezultat**? |
|---|---|---|
| Besedilo / ubeseditev | ~~N13~~ ✅, ~~N17~~ ✅, ~~N20~~ ✅, ~~N21~~ ✅ | ne — pove napačno, izračuna pravilno |
| Postavitev | N23, ~~N26~~ ✅ (+R3, R4) | ne |
| Cache na klientu | ~~N19~~ ✅ | ne (stari podatki, ne napačni) |
| Testni seed | ~~N16~~ ✅, ~~N18~~ ✅ | ne — orodje, ne izdelek |
| Pokritost testov | N2, ~~N8~~ ✅, N11 | ne sama po sebi |
| Opazljivost | N5, N6, N12 | ne — tiho, ne narobe |
| **Podatki** | ~~N1~~ ✅, ~~N9~~ ✅, ~~N14~~ ✅, ~~N15~~ ✅ | **da** — zato so šle prve |
| Frekvenca predlogov | ~~N25~~ ✅ O7 | ne — je povedalo **prevečkrat** pravilno |
| Po zasnovi / sprejeto | N10, N24 | ne |

**Bistvo:** napačen rezultat so lahko dale **štiri** od petindvajsetih, in tri od njih so bile ista
stvar — prazen `profile.timezone` (N14 vzrok, N1 simptom, N15 sorodna asimetrija). Motor ni računal
narobe; računal je pravilno na napačnem vhodu. **Vse štiri so zdaj zaprte** — dve na klientu, dve z
migracijama 0020/0021, vsaka s sondo ali testom, ki bi jo brez popravka ujel.

**Dopolnilo po sezonski simulaciji (N8 → N25 → O7):** obstaja pa peta vrsta škode, ki je ta
tabela do zdaj ni imela stolpca — **pravilen izračun, ponovljen prevečkrat**. Agronomska pravila
so bila mirna že prej (R5 1–4× na leto, R6 ×2, R2 ×1); R3 ni bil (61× in 41× na eno leto). Za
uporabnika je 129 pushov na leto enako slabo kot napačen podatek, le da se pokaže kasneje — ko
obvestila že izklopi. **Zaprto z O7**: 52 predlogov, 38 pushov, nobena straža nad 4× na leto.

### Kdaj kaj

| Kup | Najdbe | Zakaj ta kup |
|---|---|---|
| **✅ narejeno** | N7 `2b1cd49` · N16, N18 `bdf3384` · N19, N22 (delno) `9fb8d69` · **N8** (simulacija zgrajena) · **N25** (odmik ob ignoriranju, O7) · **N14 + N1** (cona), **N9**, **N15** (podatkovni paket) · **N13, N17, N20, N21, N4** (paket 3, besedilo) · **N26** (matrika je merila prazno) | |
| **A · pred prižigom** | ~~N13~~ ✅, ~~N17~~ ✅, ~~N20~~ ✅ (besedilo, ki laže) · **N23** (+R4, pogoj prižiga) · **N12** (odpovedan push se ne šteje) · ~~N4~~ ✅ (O4) | vsaka od teh se ob prižigu takoj pokaže uporabniku ali onemogoči, da bi opazili težavo |
| **B · po prižigu** | N2, N3, N11, ~~N21~~ ✅ | popravljivo brez pritiska; nič od tega ni vidno ali napačno |
| **C · sprejeto, brez ukrepa** | N5, N6 (staging, zapisano v runbooku) · N10 (zabeležena odložitev) · N24 (po zasnovi) · ostanek N22 | zavestna odločitev, ne dolg |

**Ostanek N22:** srednji »opisni pas« še vedno ne bo viden. Da bi bil, bi bilo treba ali znižati
RLS prag (kvartili kohorte šestih bi šli na napravo — poslabšanje zasebnosti), ali dodati
strežniško **redigirano** vrstico (nov stolpec/pogled). To je odločitev, ne popravek, in dokler
ni sprejeta, `config.dart` in ta dnevnik povesta resnico: **številke ali nič**.

---

## Odprto

| # | Najdba | Dokaz | Kam | Zakaj ni kozmetika |
|---|---|---|---|---|
| ~~N25~~ ✅ **O7** | **R3 nima sezonske kvote in nima odmika ob ignoriranju** — en spregledan par (subjekt, tip opravila) proizvede **40–60 kartic na leto**, in to pri uporabniku, ki ni ničesar zavrnil, samo ni ukrepal. | Sezonska simulacija (N8), 365 dni, tri vremenska semena, golden `testdata/season_calendar.ts`: **156 predlogov, 129 pushov**. Od tega **R3 `water`/bazilika ×61** in **R3 `mow`/trata ×41** = **65 % vseh kartic** in **95 od 129 pushov (74 %)**. Vsa agronomska pravila skupaj so mirna: R5 med 1× in 4× na pravilo, R6 ×2, R2 ×1. | **zaprto z O7** — odmik ob ignoriranju (`pipeline.ts` straža 5c + `housekeep.ts` `ignoredStreaks`); po popravku **52 predlogov / 38 pushov**, vsaka straža ≤ 4× na leto, R3 iz 95 na **8** pushov, R5 pa iz 31/129 na 25/38 (koristno pravilo ne tekmuje več s hrupnim za isti kanal). Decembrsko zalivanje je izginilo skupaj s tem — junij, julij, november in december so v koledarju zdaj prazni | Motor ne računa narobe — enkrat na 5 dni je natanko to, kar `03 §R3` predpisuje (`COOLDOWN: 5 dni`, `validUntil: today + 5`). Napačna je odsotnost stropa: nikjer v R1–R7 ni proračuna na sezono, in **ignoriranje ne stane nič** — `dismissed_until` nastane samo ob izrecni zavrnitvi, zato štirideseta kartica pride z enako pravico kot prva. Trije ločeni robovi: **(a)** brez `cadence_only` pravila R3 **nima nobene sezonske omejitve** — `water` na zelišču/zelenjavi nima pravila (ima ga le `lawn.water`, tedni 22–35), zato zalivanje bazilike zvoni **tudi decembra**, 5× na mesec; **(b)** besedilo kartice se izrodi: `days_overdue` raste neomejeno, januarja piše »Zalivanje zamuja **205** dni (ritem ~4 dni)«, kar je za bralca očitna okvara; **(c)** push je posledica — pri 129 pushih na leto je to obvestilo vsake 2,8 dneva, kar je natanko vzorec, po katerem uporabnik izklopi obvestila. Simulacija je edina stvar, ki to pokaže: vsak od 159 obstoječih testov gleda en `runDate`, kjer je »enkrat na 5 dni« videti razumno. |
| ~~N1~~ ✅ | **`profile.timezone` je prazen** pri uporabnikih, ki so vrt nastavili pred to funkcijo. Klient ga piše **samo** v `saveGardenLocation`. | Oba staging profila `timezone = NULL` (2026-07-28) | ✅ **zaprto z N14** (vzrok) — obstoječim produkcijskim profilom cono napolni prvi zagon popravljenega builda; **strežniški backfill ostaja odprta možnost** (strežnik ima `h3_r7` → centroid → cona), a ni več nujen in se o njem odločiva ločeno | `engine_dispatch()` izbira po `(now() at time zone coalesce(p.timezone,'UTC'))::time between 07:00 and 12:00` → tak uporabnik dobi okno **09:00–14:00 po svojem času** (poleti). `agg_event` po istem `coalesce` binira v **lokalni dan** → opravilo po 22:00 pade v napačen dan in napačen ISO teden sezonske krivulje. Strežniški backfill ni mogoč (cone ne ve); popravek je na napravi. |
| N2 | **`communityWeekly` nima testa.** | `flutter test --coverage`: vrstice 112–121 `community_providers.dart` nepokrite | **P11** | Ni scaffolding — je zanka širjenja obsega (r7→r6→r5→climate), enaka tisti, ki jo `communitySeasonCurve` ima pokrito. |
| N3 | **Detajlni zaslon Okolice nima vrstice svežine.** P8 jo je dobil samo pas na Domov. | `CommunityFeed.fetchedAt` obstaja; `SeasonCurve`/`FrequencyStats`/`CommunityWeekly` ne | lasten paket | Tri kartice pridejo iz treh ločenih rezin; uporabnik vidi številke, ne pa, iz katerega dne so. Odloženo zavestno: zahteva polje na treh modelih in predelavo `community_stats_test.dart`. |
| ~~N4~~ ✅ **O4** | **`dry_window` parameter pošilja motor, bere ga nihče.** | `candidate.ts:91` ga žigosa; `grep dry_window lib/` = 0 zadetkov, `grep` po i18n = 0 | ✅ **zaprto** — mrtvi `suggestions.weather.window_open` odstranjen iz i18n (sl/en/de) in iz generiranega `push_i18n.ts`; kartica izvornega pravila dobi pripono `suggestions.dry_window` (»Suho okno (~30 h) — primeren čas.«), ki porabi **oba** parametra (`dry_window` + `dry_hours`). Spec popravljena: `03 §R1` (R1 nima lastnega `message_key`, cooldowna, dismissa ne `validUntil`), `00-pregled-za-laika.md §0.2`, `08 §i18n`. Testa: kartica s `dry_window` pokaže pripono / brez njega je nikjer; i18n test trdi, da je `suggestions.weather` odstranjen in da pripona uporablja samo `{dry_hours}` | »Jutri je suho okno« je nosilna zgodba v `00-pregled-za-laika.md`, a do uporabnika **ne pride po nobeni poti**: mrtvi ključ `suggestions.weather.window_open` ni nikoli emitiran, pripone pa ni. Vpliva samo tiho na rangiranje. |
| N5 | **Cron na samohostanem stagingu ne more teči.** | Izvedba: `WARNING: missing engine_endpoint or engine_service_key`; nato bi ustavil še stražar izvora `^https://[a-z0-9]+\.functions\.supabase\.co/` (`0007`) | zapisano v runbooku | Ne pove ničesar: cron se vrti, `engine_run` ostane prazen, pas prazen. Za testiranje kličemo funkcijo neposredno. Če bo cron kdaj potreben na stagingu, naj dovoljeni izvor postane **podatek**, ne vzorec v kodi. |
| N6 | **Na stagingu je funkcija praktično odprta.** `isServiceRole` žeton dekodira, podpisa **ne preveri**; naslanja se na platformo. | `docker inspect supabase-edge-functions` → `VERIFY_JWT=false` | zapisano v runbooku | Sprejemljivo za lastna vrata, **ampak noben test avtorizacije na stagingu ni dokaz za produkcijo** (tam `verify_jwt = true`). |
| ~~N7~~ ✅ `2b1cd49` | **`18-nadaljevanje-prompt.md` je zastarel** — trdi, da ostajajo P8–P12. Enako velja za `20-nadaljevanje-prompt.md` po tej seji. | P8 `fec0761`, P9 `7d06944`; `20-…` predpostavlja seed s 40 sosedi in delujoč pull-to-refresh (oboje ovrženo z N18/N19) | **veljavna je `21-nadaljevanje-prompt.md`** | Vstopna točka za novo sejo; zastarela vstopna točka je slabša od nobene. Stari dve pusti, a nova seja naj bere **najvišjo številko**. |
| ~~N8~~ ✅ **zgrajena** | **Sezonske simulacije čez 365 dni ni** — točka 2 plana testiranja motorja (#12) ni bila nikoli zgrajena, dokument pa je nikoli ni označil za neizvedeno. | Vseh 10 `supabase/functions/smart-engine/*_test.ts` pregledanih: nobenega preleta `runDate` čez leto. Zadetka na »calendar« sta `rules_agro_test.ts:469` (komentar) in `signals_test.ts:201` (ime testa o sezoni) — nobeden ni simulacija | `season_sim_test.ts` + `sim_db.ts` + `synthetic_weather.ts` + `season_fixture.ts`, golden `testdata/season_calendar.ts` | 159 zelenih testov gleda vsako pravilo pri **enem** `runDate`. Pravilo, ki se sproži 40× na sezono, je s takim testom neopazno. **Kaj je meritev pokazala → N25.** Ključno za verodostojnost: `FakeDb` odgovarja iz fiksne tabele in samo beleži pisanja, torej bi zanka nad njim izmerila, kolikokrat se pravilo *lahko* sproži. Zato `SimDb`: pisanja pristanejo v isti shrambi, iz katere teče naslednji `select`, upsert **zliva** (mute preživi emit, `04 §4.3`), in `suggestion_log.last_suggested_at`, `dismissed_until`, žive `suggestion` vrstice ter `engine_run.last_push_date` gredo skozi vseh 365 dni tako kot v produkciji. |
| ~~N9~~ ✅ | **`agg_context` write-once nima DB straže, agregati pa nanj že štejejo.** | `agg_event` bere `coalesce(t.agg_context->>'h3_r7', p.h3_r7)` + r6/r5/climate: `0009:129–132`, `0017:35–38`. `before update on public.task` = **0 zadetkov** v vseh 19 migracijah. Guard samo v `tasks_repository.dart:574` (`agg_context is null` v WHERE) | ✅ **zaprto** — migracija `0020_task_agg_context_write_once.sql`: `before update of agg_context on task` **ohrani staro vrednost** namesto da bi statement zavrnil. `raise exception` je bil zavrnjen zavestno: klient potiska celo vrstico (opomba, status, pridelek), zato bi padec zataknil sinhronizacijo te vrstice za stolpec, ki ga uporabnik ne vidi — koerciranje pa agregat zaščiti v celoti, ker `agg_event` iz tega stolpca ne bere nič drugega. `raise warning` je opazovalni šiv. Izhod v sili za namerni ops poseg: `set local app.agg_context_rewrite = 'on'`. Dokaz: `supabase/probe/agg_context_invariants.sql` na stagingu — prvi žig dovoljen, prepis blokiran, ponovni push iste vrednosti brez napake, izhod v sili deluje | Invarianta, na katero se agregacija zanaša, živi v enem `where`-u enega klienta. Poti mimo nje sta **dve**: sync pull (`remote_mappers.dart:272`) prepiše `agg_context` iz oblaka brez guarda, RLS pa uporabniku dovoli update lastne `task` vrstice. Posledica ni crash, ampak **tiho preštetje istega opravila v drugo celico** ob selitvi vrta — prav scenarij, zaradi katerega posnetek obstaja. |
| N10 | **`FrequencyStats.unit` je mrtev v presentation plasti** — enaka oblika kot N4 (`dry_window`). | Agregat ima stolpec (`0009:58`, privzeto `per_season`), klient ga parsira (`community_stats.dart:239`) in nese skozi freezed model, kartica pa izriše fiksen niz `t.community.detail.freq_unit` = »na sezono« (`community_frequency_card.dart:59`) | **P10** | Odločitveno drevo #8 predpisuje **additive** `per_month` stolpec, »UI izbere enoto po tipu opravila«. Ta UI ne obstaja: nov stolpec bi pripotoval do widgeta, ki ga ignorira, in košnja bi še naprej pisala »na sezono«. **Popravek prvotnega zapisa (po branju kartice):** to **ni** spregled tipa N4 — `community_frequency_card.dart:56–58` nosi komentar, ki fiksni niz zavestno utemelji (»per-month enota bi terjala še normalizacijo mojega lastnega števca, zato pride skupaj z njo, ne prej«). Vrstica torej ostaja, a kot **zabeležena odložitev**, ne kot mrtva koda: pomembno je, da odločitveno drevo #8 to ve, sicer bo nekdo dodal stolpec in pričakoval, da se UI odzove. |
| N11 | **Klimatski normali nimajo testa na realnih lokacijah**, čeprav se #5 (10 vs 30 let) sklicuje nanj kot na obstoječ DoD. | `test/core/location/climate_service_test.dart`: 12 testov, od tega en »Ljubljana-like input« s **sintetičnimi** vrednostmi; nobene od petih imenovanih lokacij (Ljubljana, Kranjska Gora, Koper, Dunaj, München), nobenega posnetega odgovora ERA5, nobene primerjave z agro-koledarjem | lasten paket, nizka prioriteta | `kClimateHistoryYears = 10` je odločitev, ki premakne `frost_anchor`, ta pa je vhod v R2/R5 in v celotno sezonsko okno. Trenutno je edini dokaz zanjo sintetičen primer, ki ga je napisal isti algoritem, ki ga preverja. Ni nujno narobe — je **nepreverjeno**, in vprašanje #5 se brez tega testa ne da zapreti v nobeno smer. |
| N24 | **`activity_season` ne bo nikoli vseboval preteklih let** — krivulja ostane v stanju »prva sezona« do 2027. | `select year, count(*) from activity_season group by year` → **samo `2026`** (420 vrstic), čeprav seed vsakemu sosedu ustvari opravila v treh sezonah. Vzrok: `agg_refresh_all()` naredi `delete … where year = cur_year` in vstavi **samo** `cur_year` (`0009:189–207`, enako v `0018`); pretekla leta so »zamrznjena« po zasnovi, torej obstajajo le, če jih je zapisal tek **v tistem letu**. Na zaslonu: »Prva sezona: šteti so le tisti, ki so letos že začeli, zato se delež še premika« (`tmp/shots/07_detajl_jablana_polno.png`) | opažanje + popravek komentarja v seedu | Za produkcijo je to **pravilno** (agregat se nabira naprej), a ima dve posledici, ki ju nikjer ne piše: **(a)** komentar v seedu (»dve zaključeni sezoni dasta krivulji obliko«) je neresničen — ti sezoni sta nevidni, zato je krivulja testirana **samo** na tekočem letu; **(b)** ob prižigu bo vsak uporabnik do konca 2026 videl opozorilo o prvi sezoni in premikajočem se deležu. Ni napaka, je pričakovanje, ki ga je treba zavestno sprejeti — in razlog več, da se ubeseditev percentila (P4) presodi prav v tem, cenzuriranem stanju. |
| N23 | **Vremenska kartica na Domov se pri de / 320 dp / ×1,3 razsuje, in layout matrika tega ne more ujeti**, ker Domov riše brez vremena. | Naprava (`tmp/shots/13_de_320dp_x13_nav.png`, `wm density 540` = 320 dp, `font_scale 1.3`, jezik Deutsch): `29 °C` se prelomi na dve vrstici, opis »Überwiegend klar« pa se skrči na **`Ü…`** — en znak plus tropičje. Poleg tega osrednji FAB prekrije kartico »HEUTE«, besedilo »Heute keine geplanten …« pa se odreže pod navigacijo. Navigacija sama lomi `Startsei/te`, `Aufgab/en`, `Tagebu/ch`, `Umgeb/ung` (znano, R4). Vzrok za slepo pego matrike: `layout_matrix_test.dart:157` postavi `weather: null`, zato se realni nizi v testu nikoli ne izrišejo | **pogoj prižiga** (skupaj z R4) | R4 je opisal samo spodnjo vrstico; to je **isti prelom en zaslon višje in hujši** — `Ü…` ni prelom, je izguba vsebine: uporabnik ne izve, kakšno je vreme. Ključno pa je drugo: matrika je bila zgrajena prav zato, da to lovi, in tu **strukturno ne more** — testni Domov nima vremena, torej nima najdaljših nizov, ki jih ta zaslon kdaj pokaže. Dokler `weather` v matriki ostane `null`, zelena matrika za Domov ne pomeni ničesar glede vremenske kartice. |
| ~~N22~~ ✅ delno `9fb8d69` | **Srednji pas (»opisno namesto številk«) je nedosegljiv** — pod 30 kartica ne pokaže opisa, ampak **izgine**. Prag je zapisan dvakrat, na strežniku in v klientu. | RLS: `activity_frequency_read … using (n_users >= k_reliab())` in `activity_season_read … using (publishable)`, kjer `publishable` po `0018:129` zahteva `>= k_reliab()`. Strežniški `k_reliab()` bere `app_config` = **30**. Klient ima `kCommunityReliabilityMin = 30` zapečen (`config.dart:183`) in šele pod njim izriše opisno vejo `freq_low_n` (»Premalo vrtnarjev za tipičen razpon.«, `community_frequency_card.dart:64–68`). Ker sta števili enaki, vrstica z `n_users < 30` do klienta **nikoli ne pride** → veja se ne izvede nikoli. **Dokazano na napravi** v enem teku (`tmp/shots/09_prag29_apple.png` vs `09_prag29_site.png`): isti tip opravila (Gnojenje), kohorta `apple` = **29** pokaže samo kartico »Ta teden«, kohorta `@site` = **35** pokaže časovnico + kvartile + histogram. Niz `freq_low_n` se ne pojavi nikjer | **P10** | `config.dart:180–181` obljublja tri pasove (»pod `kCommunityPrivacyMin` … pod `kCommunityReliabilityMin` opisni pas, nikoli surov odstotek«), glava seeda isto, in §5b testnega načrta zahteva prav to preverbo. Dejansko sta pasova **dva**: številke ali nič. Veja `freq_low_n` je mrtva, dokler kdo ročno ne spusti `app_config.k_reliab` **pod** 30 — takrat pa začne delovati po naključju, ker klientova konstanta ne sledi. Prag, ki je nastavljiv na strežniku in zapečen v klientu, je razhod, ki čaka: `app_config` je narejen prav zato, da se **spreminja brez deploya** (odločitveno drevo #7), klient pa se brez deploya ne spremeni. |
| ~~N21~~ ✅ | **Frekvenca izpiše »2–2×«, ko sta kvartila enaka.** | Naprava, `tmp/shots/08_p6_water_brez_casovnice.png`: kartica »Kako pogosto« za `water` v kohorti jablana izpiše **`2–2×`**. Predloga `community.detail.freq_range` je v vseh treh jezikih `$from–$to×` (`community_frequency_card.dart:47–50`, zaokroženo z `_round`), brez veje za `from == to` | ✅ **zaprto** — `_range()` izbere nov ključ `community.detail.freq_single` (`$count×`), ko sta zaokroženi kvartili enaki. Test: `p25 = 2`, `p75 = 2,4` → izriše »2×«, nikoli »2–2×« | Razpon, katerega konca sta enaka, ni razpon. Ni redek rob: ravno pri **ustaljenem** opravilu (vsi ga delajo enako pogosto) sta p25 in p75 enaka — torej se pokaže prav pri najbolj tipičnem vzorcu, ne pri izjemi. Ena veja: `from == to` → »2×«. |
| ~~N20~~ ✅ | **Dvojna pika za datumom** — formatter konča s piko, predloga za njim doda še svojo. **Obseg popravljen 2026-07-28 (glej desni stolpec) — prvotni zapis je precenil škodo za 153 nizov.** | **Videno na napravi** (`tmp/shots/07_detajl_jablana_polno.png`): »Večina začne med 13. 4. in **1. 6..**«. Vir je `formatDm` (`date_format.dart:13` → `1. 6.`), ki ga `community_timing_card.dart:69–70` vloži v `community.detail.peak_weeks` (`$to.`) | ✅ **zaprto, dva ločena popravka** — **(1)** pravilo je odslej **datum nosi piko, predloga je nima**: `peak_weeks` je v vseh treh jezikih izgubil končno piko (`formatDm` jo že ima). `by_date_percent` ima datum sredi stavka in ostaja. **(2)** `fillTemplate` zna **izbirno poved v oglatih oklepajih**: `[ — okoli {frost_date}]` izpade **cela**, ko parametra ni — 5 teles × 3 jeziki popravljenih. Testi: `community_timing_card_test.dart` (vsi trije jeziki, noben izrisan `Text` ne sme vsebovati `..`; brez popravka pade) · `suggestions_i18n_test.dart` napolni vsako frost-gated telo brez `frost_date` in zavrne ` .`/`  ` (brez popravka: »… po pozebi — okoli .«) · 4 enotski testi `fillTemplate` | Ni samo estetika: ta besedila gredo tudi v **push** (`pushBody`), kjer je stavek edina vsebina. **Popravek prvotnega zapisa (preverjeno v kodi 2026-07-28):** trditev, da je prizadetih tudi ~51 teles predlogov v treh jezikih, **ne drži**. Telesa polni `suggestion_text.dart` prek **`formatDmy`**, ki se konča z letnico brez pike (`1. 6. 2026`), zato `{window_end_date}.` da pravilno `1. 6. 2026.` — ena pika. Dvojna pika nastane samo tam, kjer je vir **`formatDm`** (brez letnice). Obseg je torej: nizi, ki jih polni `formatDm`, **ne** 51×3 predlog. **Drugi rob je resničen in ločen:** `fillTemplate` manjkajoč parameter nadomesti s praznim nizom, zato **5 teles** z izbirnim `{frost_date}` (`vegetable.plant_out`, `vegetable.transplant`, `herbs.plant_out`, …) konča kot »… ko mine pozeba — okoli .« — visi presledek in pika. Motor `frost_date` pošlje le pri `rule.frost_gate && climate.lastFrostDate != null`, torej se to **zgodi v praksi**. Dva popravka, ne eden. |
| ~~N19~~ ✅ `9fb8d69` | **Pull-to-refresh v Okolici ne osveži ničesar, kar je bilo danes že prebrano** — gesta je znotraj lokalnega dneva placebo. | Naprava: po ponovnem seedu na **70** sosedov (potrjeno v bazi: `eligible 70`, `activity_season` kohorte `@site`/`apple` po 35, vse `publishable`) pristajalni zaslon **pred in po** potegu izpiše `zadnjih 7 dni · v tvoji okolici · ~40 vrtnarjev` — identičen dump obakrat. Enako na detajlu: krivulja se ne pojavi. Vzrok: `_refreshCommunity` (`community_landing_screen.dart:132–137`) in `onRefresh` na detajlu (`community_task_screen.dart:62–69`) kličeta samo `ref.invalidate(...)`; provider se res ponovno požene, a `_cachedSlice` (`community_repository.dart:445`) vrne cache, ko `_isFresh(cached)` drži, `_isFresh` (`:472–476`) pa pomeni **»pridobljeno danes«**. Mreže se ne dotakne | **P10** | Gesta obstaja, vrti se, in ne naredi nič do lokalne polnoči. Uporabnik jo potegne prav takrat, ko zaslon nekaj ne kaže (»premalo vrtnarjev«, »podatkov na tej napravi še ni«) — torej v edinem trenutku, ko bi štela. Dnevni cache je pravilna odločitev (tovor, baterija); napačno je, da ga **ročna** gesta ne more zaobiti. Ta najdba je hkrati pojasnilo, zakaj N18 tako dolgo ni bilo videti: navodilo v predaji seje (»po spremembi v bazi potegni pull-to-refresh«) ne deluje, zato je vsaka sprememba podatkov med testiranjem izgledala kot da je funkcija pokvarjena. |
| ~~N18~~ ✅ `bdf3384` | **Seed ne prestopi praga za nobeno primerjalno kohorto** — plačljivi del Okolice (krivulja časovnice, frekvenčni histogram, percentil, »Kje si ti«) je z njim **nedosegljiv**, čeprav skripta v glavi trdi nasprotno. | Naprava: detajl »Košnja« izriše **samo** kartico »Ta teden«, brez krivulje in histograma (`tmp/shots/06_detajl_kosnja.png`), pull-to-refresh ne spremeni nič. Izmerjeno v bazi za mojo celico r7 / `mow`: `plant=[''] pooled=40 pub=true` · `plant=['@site'] pooled=20 pub=false` · `apple/lettuce/pear/raspberry/tomato` po `pooled=4 pub=false`. Gate v živi funkciji je `>= k_reliab()` = **30** (`0018:52, 126, 215`), ne `k_privacy()` = 5 kot v `0009:214` | **odločitev spodaj** | Dve migraciji sta seedu spodmaknili aritmetiko, ne da bi ga kdo preračunal: **`0017`** je razdelil kohorte (`''` = kontaminirani superset, ki se za primerjavo **nikoli** ne sme uporabiti; `@site` = opravila brez katalozne rastline; `<plant>` = po rastlini), **`0018`** pa je prag krivulje dvignil na `k_reliab`. Seed deli 40 sosedov 20/20 med `@site` in rastline, rastline pa še na pet vrst po 4 — zato je edina kohorta čez 30 prav tista, ki je po `0017` prepovedana. Rezultat je najbolj zavajajoča možna kombinacija: števci so videti zdravi (`eligible 40`, `bucket_population 40`, `agg_event 1360`), Domov in »Ta teden« delujeta, plačljiva vsebina pa je prazna — in nič v nobenem izpisu ne pove, zakaj. Postavk »Sezonski tip → krivulja, ti oznaka, histogram« in »ubeseditev percentila« **ni mogoče odkljukati**, dokler seed ne zgradi vsaj ene primerjalne kohorte ≥ 30. |
| ~~N17~~ ✅ | **»Kje si ti« krivi okolico za uporabnikovo prazno zgodovino.** Ob 40 sosedih (prag 30) zaslon trdi, da jih je premalo. | Naprava, `tmp/shots/05_okolica_kje_si_ti.png`: zavihek izpiše »Za nobeno opravilo še ni dovolj vrtnarjev v okolici za primerjavo«, medtem ko `bucket_population` za mojo celico kaže `distinct_users = 40` na r7/r6/r5 in zavihek »Ta teden« ob istem času izriše tri vrstice iz istih podatkov. Vzrok: `community_providers.dart:158` `if (mine.isEmpty \|\| buckets.isEmpty) return const []` — dva različna vzroka, en prazen seznam; `community_landing_screen.dart:171–173` ima za ta primer **en sam** niz `empty_standing` | ✅ **zaprto** — `communityStandings` vrne `(rows, gap)`, kjer je `gap` `enum StandingsGap { noHistory, noSeasonalHistory, thinNeighbourhood }`; zaslon izbere niz po njem (`empty_standing_no_history` / `empty_standing_no_seasonal` / `empty_standing`), offline veja iz P8 pa še vedno pride prva. Trditev o **drugih** ostane samo v tretjem primeru. Testi: trije provider testi (prazna zgodovina · tri zalivanja pri 40 sosedih → `noSeasonalHistory` · sezonska zgodovina brez praga) + dva widget testa | P8 je ločil *offline* od *praznih podatkov* (veja `reached == false`, `:168`) — ta zlitja ni videl. Ostane najbolj zavajajoča od treh: uporabniku, ki še ni izvedel nobenega sezonskega opravila, aplikacija reče, da je soseska premajhna. To je trditev o **drugih**, ki je izmerljivo napačna, in uporabnika usmeri stran od edinega dejanja, ki bi zaslon napolnilo (izvedi opravilo). Manjka tretja veja: `mine.isEmpty` → »najprej opravi kakšno sezonsko opravilo«. **Zaostreno ob ponovitvi (isti dan, 70 sosedov):** past je širša, kot je bilo videti — uporabnik je imel **tri dokončana zalivanja**, pa je zaslon vseeno trdil, da je vrtnarjev premalo, ker `water` ni sezonski tip in ga `community_providers.dart:162` odfiltrira. Torej ne zadošča »nekaj opravi«: sporočilo laže tudi tistemu, ki **ima** zgodovino, le napačne vrste. Ob istem času je sosednji zavihek »Ta teden« izrisal štiri vrstice iz istih podatkov — dva zaslona, dve nasprotujoči si trditvi o isti soseski. |
| ~~N16~~ ✅ `bdf3384` | **Seed testnih podatkov izbere celico po izenačenju** in ob dveh testnih računih tiho seje v napačno. | Izvedba: po nastavitvi lokacije na napravi (`871e13904ffffff`) je seed izpisal `seeding 40 neighbours into r7=871e13923ffffff` — celico **drugega** računa. Vzrok: `staging_test_data.sql:63–70` združi po celici in vzame `order by count(*) desc limit 1`; ob dveh realnih profilih v dveh celicah je `count(*) = 1` pri obeh in vrstni red je poljuben | (popravljeno v tej seji) | Simptom je natanko tisti, ki ga glava te skripte navaja kot razlog svojega obstoja: »tester s tremi sosedi vidi prazne kartice in ne loči delujoče funkcije od pokvarjene«. Tu je še slabše — sosedje **so** posejani, samo drugam, zato tudi števec »40 sosedov« v izpisu potrdi uspeh, medtem ko je Okolica na napravi prazna. Neizenačen tie-break je razlika med orodjem, ki ti pove resnico, in orodjem, ki ti laže z zeleno številko. |
| ~~N14~~ ✅ | **Tihi klimatski osvežilnik na produkciji ne teče nikoli** — zato je N1 (`profile.timezone` prazen) sistemsko stanje vseh produkcijskih profilov, ne rob. | `main.dart:162`: `refreshClimateIfStale` je znotraj `if (kSuggestionsEnabled)`. `dart_defines.json` (prod) **nima ključa** `SUGGESTIONS_ENABLED` → `bool.fromEnvironment` da `false` → klic se ne izvede. Poleg tega se kliče **samo ob zagonu**, z `userId`, kakršen je takrat: pri svežem zagonu pred prijavo je to gost, po OTP prijavi pa se ne ponovi. Izmerjeno na napravi: po prijavi je profil imel `h3_r7` nastavljen, a `tz=NULL, climate=NULL` še ~7 minut, dokler nisem ročno shranil lokacije — takrat `tz=Europe/Ljubljana, climate=e1_t6` | ✅ **zaprto** — `core/location/climate_refresh_service.dart`: osvežitev je iz `kSuggestionsEnabled` ven (cona hrani tudi `agg_event.local_day` in okno pošiljanja, ne le predlogov) in se **ponovi ob vsaki spremembi prijave**, z omejenim čakanjem na profilno vrstico (`profileRowReadyForWrite`), da ne bere prazne tabele takoj po prijavi. Testi: gost brez seje, ponovitev po prijavi za nov `user_id`, brez profila = no-op | N1 je pravilno ugotovil simptom in napačno pripisal vzrok (»uporabniki, ki so vrt nastavili pred to funkcijo«). Vzrok je flag: **noben** produkcijski uporabnik nima cone, tudi tisti, ki vrt nastavijo jutri, ker klic ne obstaja v prod buildu. Trije strežniški porabniki cone (`0007:60–64` okno pošiljanja, `0009:128` in `0017:34` `local_day`) torej danes vsi tečejo na `'UTC'`. Popravek ni backfill, ampak **odstranitev napačnega pogoja**: cona in klimatski koš hranita tudi Okolico, ne le predlogov, zato je vezava prav na `kSuggestionsEnabled` napačna po vsebini. |
| ~~N15~~ ✅ | **`agg_context` zamrzne celico, ne pa cone** — polovica posnetka je zgodovinska, polovica živa. | `tasks_repository.dart:565–570` shrani `h3_r7/r6/r5` + `climate_bucket` in **nič drugega**; `agg_event` pa `local_day` računa iz `coalesce(p.timezone,'UTC')` — torej iz **trenutnega** profila (`0009:128`, `0017:34`) | ✅ **odločeno: cona gre v posnetek** — migracija `0021_agg_event_frozen_timezone.sql` (`coalesce(agg_context->>'timezone', p.timezone, 'UTC')`) + `_stampAggContext` piše `timezone`. Veriga je namenoma taka, da **obdrži obe dobri lastnosti**: stara opravila brez ključa še vedno berejo živi profil, zato jih popravek N14 retroaktivno pozdravi; nova so odporna na poznejšo selitev. Dokaz na stagingu (sonda, korak 5): isto opravilo dá `local_day` **2026-06-16** iz posnetka proti **2026-06-15** iz profila | Dve posledici, obe pomembni za odločitev o backfillu: **(a) dobra** — ko cono zapišeš, se ob naslednjem `agg_refresh_all()` retroaktivno popravi `local_day` vseh preteklih opravil, torej backfill res pozdravi zgodovino, ne le prihodnosti; **(b) slaba** — vsaka poznejša sprememba cone tiho prepiše `local_day` celotne zgodovine tega uporabnika, medtem ko celica ostane zamrznjena. »Kje« se je opravilo zgodilo, je zgodovinsko dejstvo, »kdaj« pa ne — čeprav gresta v isti agregat. Če naj bo posnetek posnetek, sodi `timezone` v `agg_context`. |
| ~~N13~~ ✅ | **»kmalu (V2)« ostane napisano, ko je funkcija prižgana** — **pet** mest obljublja prihodnost, medtem ko funkcija na istem ali sosednjem zaslonu že dela. Nobeno ni vezano na noben flag. | Naprava, staging build z **obema** flagoma `true`. Videno: uvod izpiše »Tvoja okolica / **Pozneje** poglej … / **kmalu (V2)**« (`tmp/shots/01_start.png`, uiautomator dump), tri zaslone naprej pa peti zavihek ⬡ Okolica dela in Domov kaže »V TVOJI OKOLICI · ~40 vrtnarjev« (`tmp/shots/02_domov_po_prijavi.png`). Zaslon lokacije: »… in **(kasneje)** da ti pokažemo, kaj počnejo vrtnarji v podobnem podnebju« (`tmp/shots/03_lokacija.png`). Mesta v kodi: `onboarding_screen.dart:66` (`nearby_body`) in `:67` (`soon_badge`), `location_screen.dart:235` (`location.why`), `notification_settings_screen.dart:139` (`type_weather_sub`) in `:162` (`type_community_sub`) | ✅ **zaprto** — štiri mesta dobijo drugi niz (`*_live`), izbran po flagu: uvod in zaslon lokacije po `kCommunityEnabled`, obe stikali obvestil po `kSuggestionsEnabled` (isti flag kot njun `onChanged`). Peto mesto, značka `soon_badge`, se ob prižgani Okolici **ne izriše**. Test `launch_wording_test.dart`: vsak temni niz ima živega dvojnika v vseh treh jezikih, noben živi ne vsebuje `soon/later/kmalu/pozneje/kasneje/demnächst/später/bald/(V2)`, in vsak temni ga **še vedno** vsebuje. Flagi so `const`, zato widget test lahko izriše le eno stran veje — zato je varovalo na katalogu | Pri obvestilih je `onChanged` **pravilno** vezan na `kSuggestionsEnabled` (`:144`, `:167`) — vezan ni **podnapis**: stikalo se da prestaviti, pod njim pa piše, da funkcije še ni. Uvod in zaslon lokacije sta hujša, ker nista vezana na nič: ob prižigu bo vsak nov uporabnik v prvi minuti dvakrat prebral, da je Okolica »kmalu« oz. »kasneje«, in jo takoj zatem našel v petem zavihku. To ni kozmetika — je prvi vtis, ki ga aplikacija naredi, in pove nasprotje resnice. Popravek ni prevod, ampak **odločitev, kaj naj pravi, ko je flag prižgan** (dva niza na mesto, izbrana po flagu). |
| N26 | **Layout matrika je pas predlogov hranila z neobstoječim `message_key`-em**, zato zaslona s predlogi nista bila nikoli izmerjena z resničnimi nizi. | `layout_matrix_test.dart:94` je uporabljal `'suggestions.season.window_open'`; poddrevo `suggestions` nima ključa `season` (33 otrok, preverjeno 2026-07-28). `suggestionMessage()` vrne `null` → kartica izriše **fallback** (naslov = ime tipa opravila, telo prazno). Ista datoteka je pas oglašala kot »the params that make the sentences their longest« | **P10 (popravljeno)** | Enaka oblika kot N23 (`weather: null` na Domov): matrika je zelena, ker meri prazno. Popravek na resničen ključ (`suggestions.vegetable.plant_out`, s `frost_date` in `dry_window`) je **takoj** našel prelom sredi besede — de `Auspflanzen` na `suggestions/history`, 320 dp, ×1,3, 4×. Zapisano v `docs/prelomi-besed.md` (pogoj prižiga, ne breme — zaslon je flag-dark) in v `kAcceptedWordBreaks`. Poduk je splošen: **testni fixture, ki uporablja ključ brez pokritja, ne pade — samo tiho neha meriti.** |
| N12 | **Zavrnjena dostava pusha se nikjer ne zabeleži**, medtem ko odločitveno drevo #9 visi prav na tej številki. | `handler.ts:262–268`: veja `ok === false` izniči `fcm_token` in vrne `false`; `reportError` steče **samo** v `catch`. Grep po `UNREGISTERED` v `smart-engine/`: edini zadetek je komentar v tej vrstici | **P10** (opazljivost) / kontrolni seznam prižiga | #9 pravi »telemetrija UNREGISTERED > 10 %/mes → tabela `device`«. Te telemetrije ni: žeton izgine, uporabnik neha dobivati obvestila, in nikjer ne nastane vrstica, ki bi to povedala. Simptom je enak kot pri cronu (N5) — **tišina**. Ni nujno tabela; dovolj je števec ali `engine_run` polje. |

## Razrešeno v tej seji

| # | Najdba | Kako | Hash |
|---|---|---|---|
| R1 | **`service_role` ne obide GRANT-ov, samo RLS.** `0008` trdi nasprotno; beseda `service_role` se v M11 migracijah pojavi **ničkrat**. Na okolju brez privzetih privilegijev motor umre na prvem branju in ne izdela **nobenega** predloga. | `0019_m11_engine_service_grants.sql` | `91482c3` |
| R2 | **`0006` zaseeda produkcijski `engine_endpoint`** v vsako novo okolje. | Popravljeno na stagingu + postopek v runbook; `10-odprta-vprasanja.md` #14 zaprt | `e658a2c` |
| R3 | **Layout matrika ni videla prelomov sredi besede.** 44 nizov na 11 zaslonih, 7 od njih se lomi že pri **privzeti** velikosti pisave. | Pravilo + izhodiščni seznam; breme v `docs/prelomi-besed.md` | `f79a4ea` |
| R4 | **Peti zavihek prelomi spodnjo vrstico** (de `Startseite`/`Aufgaben`/`Tagebuch`/`Umgebung`, sl `Opravila`) pri text×1,3 na 320 **in 360** dp. | Pogoj prižiga Okolice, zapisan v runbooku | `f79a4ea` |
| R5 | **En flag je prižgal brezplačne predloge in plačljivo Okolico hkrati**, ob `kDevPlusStub = true` pa jo podaril vsem. | Ločen `kCommunityEnabled`, oba iz okolja | `7d06944`, `e658a2c` |
| R6 | **`activity_recent` ostane prazen brez opravil v zadnjih 7 dneh** — sezonska zgodovina ne zadošča. | Seed dobil sveža opravila; zapisano v runbooku | (seed) |

## Procesna najdba

**V `17-plan-popravkov.md` je bil korak P7.5 (»layout matrika: `MainShell` s petimi zavihki«)
označen kot narejen, ne da bi bil.** Prav ta bi bil ujel R4. Odkljukana postavka, ki ni izvedena,
je slabša od neodkljukane — naslednji bralec je ne pogleda več.

**Posledica za način dela:** postavka se odkljuka šele, ko obstaja **artefakt**, ki to dokazuje
(test, ki pade brez popravka; vrstica v matriki; commit hash).
