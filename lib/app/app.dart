import 'package:flutter/material.dart';

class TendaskApp extends StatelessWidget {
  const TendaskApp({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      home: Scaffold(
        body: Center(
          child: Text('Tendask'),
        ),
      ),
    );
  }
}
